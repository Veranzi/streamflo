-- This migration is no longer needed.
-- The Postgres `schema.sql` already includes the role and student_grade columns.
-- Kept as a placeholder so deploy scripts that reference it don't error.
SELECT 1;
